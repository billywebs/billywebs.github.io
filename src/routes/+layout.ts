// Prerender the whole site as a static export for GitHub Pages.
export const prerender = true;
// SPA-style navigation between the (single) page — keeps it snappy.
export const trailingSlash = 'never';
